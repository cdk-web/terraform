{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Chrome Debugger",
      "request": "launch",
      "type": "pwa-chrome",
      "url": "http://localhost:9000",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "serve"
    }
  ]
}
